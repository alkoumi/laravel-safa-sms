0.3.0
- Added `removeDuplicate()` to remove duplicated numbers to send `Rmduplicated` with the rquest to www.safa-sms.com to remove duplicated numbers.
- Added `notifyAdmin()` to send email to admin if any error happend after sending the request.
